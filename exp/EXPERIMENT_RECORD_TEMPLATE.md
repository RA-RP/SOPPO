# <experiment ID>：<简短名称>

> 将本文件复制为 `work/exp/<experiment_id>/README.md`，并替换所有占位符。

## 元信息

- Experiment ID：`exp-YYYYMMDD-NN-short-name`
- Cycle ID：`cycle-YYYYMMDD-NN`
- 状态：`LOCKED` / 计划中 / 运行中 / 已完成 / 失败 / 中断
- 对应实验设计：`../../human_read/exp/current_experiment.md` 或归档锚点
- 对应理论：`../../human_read/theory/current_theory.md` 或归档锚点
- 理论批准版本：
- 理论用户确认日期：
- 实验设计批准版本：
- 实验设计用户确认日期：
- 代码版本/静态校验值：
- 代码交接状态：已确认 / 未确认
- 代码交接用户确认日期：
- 服务器执行授权：已明确授权 / 无授权
- 执行授权日期：
- 执行时间与负责人：
- 执行位置：服务器任务（本地禁止执行）
- 服务器运行手册：

> 上述理论批准、实验批准、代码版本、代码交接与执行授权字段任一缺失时，本记录状态只能为 `LOCKED`，不得创建或提交服务器任务。用户沉默、未反对或普通讨论不能代替明确确认。

## 要回答的问题

- 假设：
- 对照与变量：
- 主要指标：
- 预先约定的判断标准：

## 实际运行

- 服务器任务名/ID：
- 服务器代码路径：
- 服务器共享挂载：
- 数据版本与切分：
- 最终生效配置：
- 服务器命令/入口：
- 随机种子：
- 服务器软件环境/镜像：
- 硬件：
- 运行时长：

## 产物索引

| 产物 | 服务器路径或获取方式 | 本地是否保留 | 说明 |
| --- | --- | --- | --- |
| 配置快照 |  | 是（文本） |  |
| 环境摘要/校验值 |  | 是（文本） | 不回传环境或包目录 |
| 聚合指标 |  | 是 | 不含逐样本行 |
| 汇总图表 |  | 是 | 只保留聚合图 |
| 原始日志 |  | 否 | 本地只记录任务/日志索引；可能含样本文本 |
| 逐样本预测 |  | 否 | 服务器保留 |
| 数据/私有标签 |  | 否 | 服务器保留 |
| 模型/adapter/checkpoint |  | 否 | 服务器保留 |

## 本地回传边界

- 已回传白名单文件：
- 服务器保留黑名单产物：
- 本地仅记录的远程路径和校验值：
- 确认本地未运行环境、测试、数据、模型、评价或 GPU 任务：是 / 否

## 事实摘要

只根据服务器生成的聚合摘要写直接观察到的事实、运行偏差与异常；不得在本地重新计算。完整解释写入 `../../human_read/result/current_result.md`。

- 关键数值：
- 与计划的偏差：
- 异常/失败：
- 对应结果记录：

## 结果交接

- 结果文档版本：
- 结果完整快照归档：是 / 否
- 用户明确确认结果交接完成：是 / 否
- 交接确认日期：
- 下一轮讨论是否解锁：是 / 否
