_base_: base.yaml

method:
  name: "dpo100"
  use_unlabeled: false

training:
  epochs: 1
  lr: 0.000001

# The isolated materializer creates oracle_train.private.jsonl. Validation and
# test examples never participate in gradients.
