# Candidate vLLM replica stack. Exact successful versions are frozen by the
# authorized server into the external Round3 environment manifest.
vllm>=0.9.2,<0.10
transformers>=4.53,<4.54
peft>=0.15,<0.17
pyyaml>=6,<7
packaging>=24,<26

